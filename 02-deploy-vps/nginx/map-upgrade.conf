# Goes in the http{} block (e.g. /etc/nginx/conf.d/00-map-upgrade.conf).
# $connection_upgrade must be "upgrade" for websocket requests and "close"
# otherwise. Hardcoding Connection "upgrade" breaks keepalive for normal requests.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
