# Copy to .env and fill in. chmod 600 .env — and never commit it.
NODE_ENV=production
PORT=3000

POSTGRES_DB=myapp
POSTGRES_USER=myapp
POSTGRES_PASSWORD=

DATABASE_URL=postgres://myapp:@db:5432/myapp

# Set by CI to the commit SHA so every deploy is traceable and reversible.
TAG=local
